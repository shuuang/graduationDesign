var db = require('../dbutil/db').db;
var clubuser=require('../models/clubuser')(db);
const {Op} = require("sequelize");

class ClubMember {
    constructor(body) {
        this.cmid=this.cmid;
        this.cid=this.cid;
        this.num=this.num;
        this.year=this.year;
    }
    async 
}